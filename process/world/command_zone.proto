syntax = "proto3";

package Proto;

import "process/world/player_route.proto";

// World -> Zone: 
service WorldCommand {
  // World -> Zone: player begin zone exit request.
  rpc BeginZoneExitCommand(PayloadPlayerZoneEnterBegin) returns (PayloadPlayerRouteResult);
}
