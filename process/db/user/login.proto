syntax = "proto3";

package Proto;

import "common/math.proto";
import "common/session.proto";

message PayloadLoginRequest {
  SessionId session_id = 1;
}

message PayloadLoginResponse {
  bool   is_succeeded = 1;
  uint32 user_id = 2;
  SessionId session_id = 3;
}
